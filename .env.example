# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# Node Environment (development/production)
NODE_ENV=production

# Server Port (Heroku will set this automatically)
PORT=3001

# Google Sheets Configuration
GOOGLE_SHEET_ID=your_google_sheet_id_here
SHEET_NAME=Sign-ups

# Google Authentication - Use ONE of the following methods:

# Method 1: JSON credentials as environment variable (RECOMMENDED for Heroku)
# Get this by running: cat backend/credentials.json | tr -d '\n' | tr -d ' '
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project",...}

# Method 2: File path (for local development only)
# GOOGLE_CREDENTIALS_FILE=./backend/credentials.json
